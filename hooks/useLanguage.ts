/**
 * Re-export useLanguage from LanguageContext
 * This maintains backward compatibility while using the context-based approach
 */
export { useLanguage, type Language } from '../contexts/LanguageContext';
